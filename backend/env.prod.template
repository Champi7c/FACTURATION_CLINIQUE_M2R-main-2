# =============================================================================
# FICHIER DE CONFIGURATION PRODUCTION - .env.prod
# =============================================================================
# INSTRUCTIONS:
# 1. Copier ce fichier vers .env.prod
# 2. Remplacer TOUTES les valeurs par vos vraies valeurs
# 3. NE JAMAIS commiter .env.prod dans Git
# 4. Ajouter .env.prod à .gitignore
# =============================================================================

# -----------------------------------------------------------------------------
# DJANGO SETTINGS
# -----------------------------------------------------------------------------
# IMPORTANT: Générer une nouvelle clé secrète pour la production
# Commande: python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CHANGEZ_MOI_AVEC_UNE_CLE_SECRETE_DE_50_CARACTERES_MINIMUM

# IMPORTANT: TOUJOURS False en production
DEBUG=False

# Liste des domaines autorisés (séparés par des virgules)
ALLOWED_HOSTS=votre-domaine.com,127.0.0.1,localhost

# -----------------------------------------------------------------------------
# DATABASE SETTINGS
# -----------------------------------------------------------------------------
DB_NAME=facturation_clinique
DB_USER=facturation_user
DB_PASSWORD=CHANGEZ_MOI_MOT_DE_PASSE_FORT_32_CARACTERES
DB_ROOT_PASSWORD=CHANGEZ_MOI_MOT_DE_PASSE_ROOT_FORT
DB_HOST=db
DB_PORT=3306

# -----------------------------------------------------------------------------
# CORS SETTINGS
# -----------------------------------------------------------------------------
# Origines autorisées pour les requêtes CORS (votre domaine frontend)
CORS_ALLOWED_ORIGINS=https://votre-domaine.com

# Ne PAS mettre True en production
CORS_ALLOW_ALL_ORIGINS=False

